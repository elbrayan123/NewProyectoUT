<%= form_for(post_vacante) do |f| %>
  <% if post_vacante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_vacante.errors.count, "error") %> prohibited this post_vacante from being saved:</h2>

      <ul>
      <% post_vacante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :carrera %>
    :
    <%= f.text_field :carrera %>
  </div>

  <div class="field">
    <%= f.label :area %>
    :
    <%= f.text_field :area %>
  </div>

  <div class="field">
    <%= f.label :numero_de_vacantes %>
    :
    <%= f.text_field :nvacantes %>
  </div>

  <div class="field">
    <%= f.label :descripción %>
    :
    <%= f.text_area :descripcion %>
  </div>


   <div >
    <%= f.submit 'Crear', class: "btn btn-success" %>
   </div>
   
<% end %>
